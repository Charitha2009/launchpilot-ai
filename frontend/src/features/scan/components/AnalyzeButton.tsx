/**
 * Presentational submit control — no form or service logic.
 */
export interface AnalyzeButtonProps {
  loading?: boolean;
  disabled?: boolean;
}

export default function AnalyzeButton(_props: AnalyzeButtonProps) {
  // TODO: Render shadcn Button ("Analyze"); reflect loading/disabled.
  return null;
}
