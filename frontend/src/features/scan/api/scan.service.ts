/**
 * ScanService — framework-free application API for starting a scan.
 * Layer: useScan → scan.service → lib/api (ADR-002).
 * TODO: Implement createScan / getScan without React imports.
 */

export {};
